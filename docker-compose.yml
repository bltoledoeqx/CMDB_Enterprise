services:
  cmdb:
    build: .
    container_name: cmdb-enterprise
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - cmdb-data:/data
      - /opt/livros:/livros
    environment:
      - PORT=3000
      - DATA_DIR=/data
      - BOOKS_DIR=/livros
      - KAVITA_URL=http://kavita:5000
      - SESSION_SECRET=mude-esta-chave-em-producao-2024
    depends_on:
      kavita:
        condition: service_healthy
    networks:
      - npm_proxy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/api/auth/me"]
      interval: 30s
      timeout: 5s
      retries: 3

  kavita:
    image: jvmilazz0/kavita:latest
    container_name: kavita
    restart: unless-stopped
    volumes:
      - /opt/livros:/manga
      - kavita-config:/kavita/config
      - /opt/kavita-settings/appsettings.json:/kavita/config/appsettings.json
    networks:
      - npm_proxy
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:5000/api/health"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 20s

networks:
  npm_proxy:
    external: true

volumes:
  cmdb-data:
    driver: local
  kavita-config:
    driver: local
